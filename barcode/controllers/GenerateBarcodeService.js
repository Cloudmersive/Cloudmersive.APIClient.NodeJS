'use strict';


/**
 * Validates and generate a EAN-13 barcode as a PNG file, a type of 1D barcode
 *
 * value String Barcode value to generate from
 * no response value expected for this operation
 **/
exports.generateBarcode_EAN13 = function(value) {
  return new Promise(function(resolve, reject) {
    resolve();
  });
}


/**
 * Validates and generate a EAN-8 barcode as a PNG file, a type of 1D barcode
 *
 * value String Barcode value to generate from
 * no response value expected for this operation
 **/
exports.generateBarcode_EAN8 = function(value) {
  return new Promise(function(resolve, reject) {
    resolve();
  });
}


/**
 * Generate a QR code barcode as a PNG file, a type of 2D barcode which can encode free-form text information
 *
 * value String QR code text to convert into the QR code barcode
 * no response value expected for this operation
 **/
exports.generateBarcode_QRCode = function(value) {
  return new Promise(function(resolve, reject) {
    resolve();
  });
}


/**
 * Validate and generate a UPC-A barcode as a PNG file, a type of 1D barcode
 *
 * value String UPC-A barcode value to generate from
 * no response value expected for this operation
 **/
exports.generateBarcode_UPCA = function(value) {
  return new Promise(function(resolve, reject) {
    resolve();
  });
}


/**
 * Validates and generate a UPC-E barcode as a PNG file, a type of 1D barcode
 *
 * value String UPC-E barcode value to generate from
 * no response value expected for this operation
 **/
exports.generateBarcode_UPCE = function(value) {
  return new Promise(function(resolve, reject) {
    resolve();
  });
}

